<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Base.Theme.EcoTrujilloApp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/white</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@android:color/white</item>
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:fontFamily">@font/sf_pro_display</item>


        <!-- Mantener compatibilidad visual -->
        <item name="colorPrimary">@android:color/white</item>
        <item name="colorOnPrimary">@android:color/black</item>
    </style>

    <style name="Theme.EcoTrujilloApp" parent="Base.Theme.EcoTrujilloApp" />

</resources>
